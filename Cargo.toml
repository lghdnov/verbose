[package]
name = "verbose"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
inventory = "0.3"

[features]
default = ["lang-ru", "lang-en"]
lang-ru = []
lang-en = []

[profile.release]
opt-level = 3
lto = true
